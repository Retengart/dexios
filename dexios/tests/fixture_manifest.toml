[[fixture]]
id = "phase01-cli-corrupted-stream"
group = "phase01-cli-workflow-corruption"
path = "dexios/tests/decrypt_cli_regressions.rs::decrypt_cli_corrupted_stream_variants_preserve_existing_output"
purpose = "Prove CLI decrypt rejects final tamper, one-byte truncation, and reordered normal stream chunks without replacing an existing output."
invariant = "D-15"
requirement = "STRM-02"
source = "Generated by the CLI integration test with the compiled dexios binary, disposable keyfiles, disposable multiblock plaintext, and deterministic payload mutations after HEADER_LEN."
expected = "dexios decrypt --force exits non-success with terse typed stderr and the pre-existing output sentinel remains unchanged."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-cli-corrupted-archive-stream"
group = "phase01-cli-workflow-corruption"
path = "dexios/tests/unpack_cli_regressions.rs::unpack_cli_corrupted_archive_never_extracts_outputs"
purpose = "Prove CLI unpack rejects corrupted encrypted archive streams before extracting safe archive entries."
invariant = "D-15"
requirement = "STRM-02"
source = "Generated by the CLI unpack regression test from a disposable ZIP archive encrypted with a disposable key, then final-tampered or one-byte truncated after HEADER_LEN."
expected = "dexios unpack -f exits non-success with terse typed stderr and the safe archive entry is not extracted."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-cli-archive-path-symlink-component"
group = "phase01-cli-archive-path-assurance"
path = "dexios/tests/unpack_cli_regressions.rs::unpack_cli_rejects_symlinked_output_component"
purpose = "Link existing CLI archive-path symlink component rejection into the Phase 01 provenance corpus."
invariant = "D-04"
requirement = "ASSR-02"
source = "Generated by the CLI unpack integration test with disposable directories and a symlinked output component on supported platforms."
expected = "dexios unpack -f exits non-success with an unsafe-path message and writes no file outside the output root."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-cli-archive-path-duplicate-normalized-targets"
group = "phase01-cli-archive-path-assurance"
path = "dexios/tests/unpack_cli_regressions.rs::unpack_cli_rejects_duplicate_normalized_targets"
purpose = "Link existing CLI archive-path duplicate-normalization rejection into the Phase 01 provenance corpus."
invariant = "D-04"
requirement = "ASSR-02"
source = "Generated by the CLI unpack integration test from a disposable ZIP archive with two names that normalize to the same output path."
expected = "dexios unpack -f exits non-success with a duplicate-path message and does not extract the colliding target."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-cli-generated-corruption-promoted"
group = "phase01-generated-case-promotion"
path = "dexios/tests/decrypt_cli_regressions.rs::decrypt_cli_corrupted_stream_variants_preserve_existing_output"
purpose = "Prove generated CLI corruption variants are promoted into normal deterministic Cargo regressions rather than live random exploration."
invariant = "D-07"
requirement = "ASSR-03"
source = "Deterministic final-tamper, one-byte-truncation, and reordered-normal-chunk mutations are fixed in source and replayed by decrypt_cli_regressions."
expected = "The generated corruption variants are exercised through cargo test --locked --offline -p dexios --test decrypt_cli_regressions --release."
owner_phase = "Phase 1"
