[[fixture]]
id = "canonical-v1-valid-single-keyslot"
group = "canonical-v1"
path = "dexios-core/tests/v1_header.rs::serializes_sample_v1_header_to_exact_bytes"
purpose = "Prove the canonical V1 512-byte header layout for one active physical keyslot."
invariant = "FMT-01"
requirement = "FMT-01"
source = "Serialized from dexios-core tests support::sample_v1_header(); reviewable source asserts the exact DXIO 00 01 CV1\\0 static header, 64-byte AAD, and 112-byte physical slot records."
expected = "dexios_core::header::read_header succeeds, reports one canonical V1 keyslot, produces 64 bytes of immutable AAD, and serializes back to identical bytes."
owner_phase = "Phase 3"

[[fixture]]
id = "retired-current-v1-valid-single-keyslot"
group = "obsolete-format-rejection"
path = "dexios-core/tests/testdata/v1_valid_single_keyslot.hex"
purpose = "Retain the old current 416-byte V1 positive fixture only as obsolete-format rejection evidence."
invariant = "FMT-02"
requirement = "FMT-02"
source = "Old current V1 hex fixture retained from Phase 2; it lacks the canonical CV1\\0 discriminator and is not positive canonical evidence."
expected = "dexios_core::header::read_header fails with HeaderReadError::RetiredV1Layout."
owner_phase = "Phase 3"

[[fixture]]
id = "retired-current-v1-malformed-reserved-byte"
group = "obsolete-format-rejection"
path = "dexios-core/tests/testdata/v1_malformed_reserved_byte.hex"
purpose = "Retain the old current malformed 416-byte V1 fixture only as obsolete-format rejection evidence."
invariant = "FMT-02"
requirement = "FMT-02"
source = "Copied from the old current V1 valid fixture and mutated before canonical V1 existed; it lacks the canonical CV1\\0 discriminator."
expected = "dexios_core::header::read_header fails with HeaderReadError::RetiredV1Layout."
owner_phase = "Phase 3"

[[fixture]]
id = "canonical-v1-parser-table"
group = "canonical-v1-malformed"
path = "dexios-core/tests/v1_header.rs::parser_table_rejects_canonical_v1_malformed_metadata_by_variant"
purpose = "Prove canonical V1 parser failures preserve exact typed variants for retired layout, discriminator, schema, reserved bytes, payload metadata, KDF metadata, slot state, physical slot index, and truncation."
invariant = "FMT-05"
requirement = "FMT-05"
source = "Generated in memory from the canonical V1 sample header with one mutation per parser table case."
expected = "Each parser table case fails with the exact HeaderReadError variant asserted by the test."
owner_phase = "Phase 3"

[[fixture]]
id = "canonical-v1-zero-keyslot-count"
group = "canonical-v1-malformed"
path = "dexios-core/tests/v1_header.rs::v1_header_rejects_zero_keyslots"
purpose = "Prove canonical V1 construction and parsing reject headers with no active keyslots."
invariant = "KEY-001"
requirement = "KEY-01"
source = "Generated from the canonical V1 header test support builder and mutated in memory to remove active slots."
expected = "V1Keyslots::try_from_vec rejects empty collections and read_header rejects zero active canonical keyslots."
owner_phase = "Phase 3"

[[fixture]]
id = "canonical-v1-invalid-kdf-profile"
group = "canonical-v1-malformed"
path = "dexios-core/tests/v1_header.rs::v1_header_rejects_invalid_kdf_tag"
purpose = "Prove canonical V1 parsing rejects unknown active-keyslot KDF profile metadata."
invariant = "FMT-05"
requirement = "FMT-05"
source = "Generated from the canonical V1 header test support builder and mutated in memory at the active keyslot KDF profile byte."
expected = "dexios_core::header::read_header fails with HeaderReadError::InvalidKdfProfile."
owner_phase = "Phase 3"

[[fixture]]
id = "legacy-v5-unsupported-format"
group = "malformed-v1"
path = "dexios-core/tests/v1_header.rs::read_header_rejects_legacy_prefix_as_unsupported_format"
purpose = "Prove legacy Dexios V5 headers are intentionally rejected by the V1-only reader."
invariant = "FMT-006"
requirement = "FORM-04"
source = "In-memory legacy V5 prefix [DE, 05] with legacy algorithm and mode bytes."
expected = "dexios_core::header::read_header fails with HeaderReadError::UnsupportedFormat([0xDE, 0x05])."
owner_phase = "Phase 2"

[[fixture]]
id = "canonical-v1-wrong-key"
group = "wrong-key"
path = "dexios-domain/tests/keyslots_crypto_v1.rs"
purpose = "Prove generated canonical V1 encrypted fixtures fail verification or decryption with a different disposable key."
invariant = "KEY-002"
requirement = "VERI-02"
source = "Generated inside the dexios-domain keyslots_crypto_v1 integration test with disposable in-memory keys and payload."
expected = "Verification and decryption with the wrong disposable key fail before plaintext is accepted."
owner_phase = "Phase 3"

[[fixture]]
id = "canonical-v1-detached-header"
group = "detached-header"
path = "dexios/tests/header_details_cli.rs"
purpose = "Prove canonical V1 detached-header CLI output keeps header bytes separate from encrypted payload bytes."
invariant = "FMT-005"
requirement = "FMT-01"
source = "Generated by the dexios CLI integration test using disposable files and DEXIOS_KEY."
expected = "The detached header is exactly CANONICAL_HEADER_LEN parseable bytes and the payload file no longer starts with the V1 header magic."
owner_phase = "Phase 3"

[[fixture]]
id = "keyslot-mutation-two-keyslots"
group = "keyslot-mutation"
path = "dexios-domain/tests/keyslots_mutation_v1.rs"
purpose = "Prove canonical V1 fixed physical keyslot mutation workflows can add, verify, change, and delete keyslots while preserving payload bytes and another decrypting key."
invariant = "FMT-04"
requirement = "KEY-003"
source = "Generated inside dexios-domain keyslots_mutation_v1 integration test with disposable in-memory old, new, and third keys."
expected = "The added and changed keyslots stay in fixed physical slots, the deleted slot is cleared without compaction, and remaining keys still decrypt the payload."
owner_phase = "Phase 3"

[[fixture]]
id = "phase01-stream-payload-boundary-matrix"
group = "phase01-stream-assurance"
path = "dexios-core/tests/stream_v1.rs::stream_payload_boundary_matrix_roundtrips_with_file_api"
purpose = "Prove V1 file stream roundtrips cover empty, one-byte, block-minus-one, exact-block, block-plus-one, exact multi-block, and multi-block-plus-tail payloads."
invariant = "D-12"
requirement = "STRM-01"
source = "Generated deterministically by payload_boundary_cases with non-secret byte patterns and replayed by the core stream_v1 integration test."
expected = "Every payload boundary case encrypts and decrypts through V1PayloadStream::encrypt_file and decrypt_file without byte drift."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-fragmented-io-short-output"
group = "phase01-stream-assurance"
path = "dexios-core/tests/stream_v1.rs::stream_file_api_handles_short_output_writes_at_boundaries"
purpose = "Prove V1 file stream APIs tolerate short plaintext reads, short ciphertext reads, and short output writes across normal and final boundaries."
invariant = "D-13"
requirement = "STRM-01"
source = "Generated deterministically from payload_boundary_cases with ShortRead and ShortWrite wrappers, including write chunks smaller than STREAM_TAG_LEN."
expected = "All boundary payloads roundtrip when encryption and decryption outputs accept only small partial writes."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-wrong-aad"
group = "phase01-stream-corruption"
path = "dexios-core/tests/stream_v1.rs::decrypt_rejects_mismatched_header_aad"
purpose = "Prove payload ciphertext is authenticated against the parsed canonical V1 static header AAD and rejects a mismatched immutable context."
invariant = "D-14"
requirement = "STRM-02"
source = "Generated in memory from sample V1 headers with matching nonce but different static header-derived AAD; no committed secret material."
expected = "V1PayloadStream::decrypt_file_uncommitted returns StreamError::Authentication and does not report committed plaintext success."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-wrong-nonce"
group = "phase01-stream-corruption"
path = "dexios-core/tests/stream_v1.rs::decrypt_rejects_wrong_payload_nonce"
purpose = "Prove V1 stream ciphertext is bound to the payload nonce from the parsed header."
invariant = "D-14"
requirement = "STRM-02"
source = "Generated in memory by decrypting deterministic ciphertext with a parsed payload carrying a different nonce."
expected = "V1PayloadStream::decrypt_file_uncommitted returns StreamError::Authentication and does not expose committed plaintext success."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-wrong-key"
group = "phase01-stream-corruption"
path = "dexios-core/tests/stream_v1.rs::decrypt_rejects_wrong_master_key"
purpose = "Prove deterministic V1 stream ciphertext fails when decrypted with the wrong disposable master key."
invariant = "D-14"
requirement = "STRM-02"
source = "Generated in memory from sample V1 headers and deterministic payload bytes using disposable fixed test keys."
expected = "V1PayloadStream::decrypt_file_uncommitted returns StreamError::Authentication before committed plaintext success."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-reordered-chunks"
group = "phase01-stream-corruption"
path = "dexios-core/tests/stream_v1.rs::decrypt_rejects_reordered_chunks"
purpose = "Prove V1 LE31 chunk ordering is authenticated and rejects swapped non-final chunks."
invariant = "D-14"
requirement = "STRM-02"
source = "Generated in memory by encrypting plaintext_spanning_normal_chunks and swapping two deterministic ciphertext chunks."
expected = "V1PayloadStream::decrypt_file_uncommitted returns StreamError::Authentication before committed plaintext success."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-duplicated-chunk"
group = "phase01-stream-corruption"
path = "dexios-core/tests/stream_v1.rs::decrypt_rejects_duplicated_chunk"
purpose = "Prove V1 stream decryption rejects a duplicated non-final ciphertext chunk inserted after the original."
invariant = "D-14"
requirement = "STRM-02"
source = "Generated in memory by encrypting plaintext_spanning_normal_chunks and inserting a duplicate of the first non-final chunk."
expected = "V1PayloadStream::decrypt_file_uncommitted returns StreamError::Authentication; any bytes written are uncommitted scratch before the duplicate."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-truncated-chunks"
group = "phase01-stream-corruption"
path = "dexios-core/tests/stream_v1.rs::decrypt_rejects_each_truncated_stream_chunk"
purpose = "Prove every encrypted V1 stream chunk index fails when one byte is removed."
invariant = "D-14"
requirement = "STRM-02"
source = "Generated in memory by encrypting plaintext_spanning_normal_chunks and replaying each chunk with a deterministic one-byte truncation."
expected = "Each truncated chunk produces a decrypt error and only uncommitted scratch before the failed chunk."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-missing-final-block"
group = "phase01-stream-corruption"
path = "dexios-core/tests/stream_v1.rs::decrypt_rejects_missing_final_block"
purpose = "Prove exact multi-block V1 streams reject ciphertext that omits the authenticated final marker."
invariant = "D-14"
requirement = "STRM-02"
source = "Generated in memory by removing the deterministic empty final marker after exact-two-block encryption."
expected = "V1PayloadStream::decrypt_file_uncommitted returns StreamError::MissingFinalBlock; plaintext bytes are uncommitted scratch."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-middle-tamper"
group = "phase01-stream-corruption"
path = "dexios-core/tests/stream_v1.rs::decrypt_rejects_tampered_middle_chunk"
purpose = "Prove V1 stream decryption rejects a bit flip in a non-final ciphertext chunk."
invariant = "D-14"
requirement = "STRM-02"
source = "Generated in memory by flipping a deterministic bit in the second encrypted chunk of plaintext_spanning_normal_chunks."
expected = "V1PayloadStream::decrypt_file_uncommitted returns StreamError::Authentication; earlier plaintext is only uncommitted scratch."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-stream-final-tamper"
group = "phase01-stream-corruption"
path = "dexios-core/tests/stream_v1.rs::decrypt_rejects_tampered_final_chunk"
purpose = "Prove V1 stream decryption rejects a bit flip in the authenticated final ciphertext chunk."
invariant = "D-14"
requirement = "STRM-02"
source = "Generated in memory by flipping a deterministic bit in the final encrypted chunk of plaintext_spanning_normal_chunks."
expected = "V1PayloadStream::decrypt_file_uncommitted returns StreamError::FinalBlockAuthentication; normal chunks are only uncommitted scratch."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-header-malformed-evidence"
group = "phase01-header-assurance"
path = "dexios-core/tests/v1_header.rs::fixture_v1_malformed_reserved_byte_is_rejected"
purpose = "Link existing reviewable malformed V1 header evidence into the Phase 1 assurance corpus."
invariant = "D-04"
requirement = "ASSR-02"
source = "Checked-in lowercase hex fixture v1_malformed_reserved_byte.hex derived from the valid single-keyslot V1 header by one reserved-byte mutation."
expected = "dexios_core::header::read_header rejects the fixture with HeaderReadError::NonZeroReservedBytes."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-keyslot-corruption-evidence"
group = "phase01-keyslot-assurance"
path = "dexios-domain/tests/keyslots_crypto_v1.rs::wrong_key_current_v1_fixture_rejects_verification_and_decrypt"
purpose = "Link existing generated keyslot wrong-key evidence into the Phase 1 assurance corpus."
invariant = "D-04"
requirement = "ASSR-02"
source = "Generated inside dexios-domain keyslots_crypto_v1 with disposable in-memory keys and payload; no committed secret material."
expected = "Key verification returns IncorrectKey and decrypt returns DecryptMasterKey without accepting plaintext."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-core-generated-stream-cases-promoted"
group = "phase01-generated-case-promotion"
path = "dexios-core/tests/stream_v1.rs::payload_boundary_cases"
purpose = "Prove generated core stream boundary cases are promoted into normal deterministic Cargo regressions rather than live random exploration."
invariant = "D-07"
requirement = "ASSR-03"
source = "Deterministic payload lengths and byte patterns are fixed in source and replayed by the stream_v1 integration test."
expected = "The generated boundary cases are exercised through cargo test --locked --offline -p dexios-core --test stream_v1 --release."
owner_phase = "Phase 1"
