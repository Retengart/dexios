[[fixture]]
id = "v1-valid-single-keyslot"
group = "valid-v1"
path = "dexios-core/tests/testdata/v1_valid_single_keyslot.hex"
purpose = "Prove the current byte-stable V1 header parse and serialize baseline for one active keyslot."
invariant = "FMT-001"
requirement = "VERI-02"
source = "Serialized from dexios-core tests support::sample_v1_header(); reviewable lowercase hex records the exact current bytes."
expected = "dexios_core::header::read_header succeeds, reports one V1 keyslot, produces 32 bytes of AAD, and serializes back to identical bytes."
owner_phase = "Phase 2"

[[fixture]]
id = "v1-malformed-reserved-byte"
group = "malformed-v1"
path = "dexios-core/tests/testdata/v1_malformed_reserved_byte.hex"
purpose = "Prove V1 parsing rejects a non-zero reserved byte without accepting malformed header metadata."
invariant = "FMT-001"
requirement = "VERI-02"
source = "Copied from v1-valid-single-keyslot and mutated at byte index 7 from 00 to 01."
expected = "dexios_core::header::read_header fails with HeaderReadError::NonZeroReservedBytes."
owner_phase = "Phase 2"

[[fixture]]
id = "wrong-key-current-v1"
group = "wrong-key"
path = "dexios-domain/tests/keyslots_v1.rs"
purpose = "Prove generated current V1 encrypted fixtures fail verification or decryption with a different disposable key."
invariant = "STRM-001"
requirement = "VERI-02"
source = "Generated inside the dexios-domain keyslots_v1 integration test with disposable in-memory keys and payload."
expected = "Verification and decryption with the wrong disposable key fail before plaintext is accepted."
owner_phase = "Phase 3"

[[fixture]]
id = "detached-header-current-v1"
group = "detached-header"
path = "dexios/tests/header_details_cli.rs"
purpose = "Prove current V1 detached-header CLI output keeps header bytes separate from encrypted payload bytes."
invariant = "FMT-002"
requirement = "VERI-02"
source = "Generated by the dexios CLI integration test using disposable files and DEXIOS_KEY."
expected = "The detached header is parseable as a V1 header and the payload file no longer starts with the V1 header magic."
owner_phase = "Phase 3"

[[fixture]]
id = "keyslot-mutation-two-keyslots"
group = "keyslot-mutation"
path = "dexios-domain/tests/keyslots_v1.rs"
purpose = "Prove generated current V1 keyslot mutation workflows can add, verify, change, and delete keyslots while preserving another decrypting key."
invariant = "KEY-001"
requirement = "VERI-02"
source = "Generated inside dexios-domain keyslots_v1 integration test with disposable in-memory old, new, and third keys."
expected = "The added and changed keys verify, the deleted key no longer verifies, and the original key still decrypts the payload."
owner_phase = "Phase 5"
