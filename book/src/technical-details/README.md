# Technical Details

This section documents the behavior of the current Dexios implementation rather than the historical website alone. If a statement here conflicts with the running code, the code should be treated as the source of truth until the docs are updated.
