[[fixture]]
id = "phase01-domain-corrupted-stream"
group = "phase01-domain-workflow-corruption"
path = "dexios-domain/tests/decrypt_workflow_errors.rs::decrypt_corrupted_stream_variants_never_commit_final_output"
purpose = "Prove domain decrypt rejects final tamper, one-byte truncation, and reordered normal stream chunks without committing final plaintext over an existing output."
invariant = "D-15"
requirement = "STRM-02"
source = "Generated in the domain integration test from disposable plaintext larger than three V1 stream blocks, then mutated after HEADER_LEN with deterministic byte operations."
expected = "decrypt::execute returns an authentication-class failure and the pre-existing sentinel output bytes remain unchanged."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-domain-corrupted-archive-stream"
group = "phase01-domain-workflow-corruption"
path = "dexios-domain/tests/unpack.rs::unpack_corrupted_stream_never_extracts_outputs"
purpose = "Prove domain unpack rejects corrupted encrypted archive streams before extracting safe archive entries."
invariant = "D-15"
requirement = "STRM-02"
source = "Generated in the domain unpack integration test from a disposable ZIP archive encrypted with a disposable key, then final-tampered or one-byte truncated after HEADER_LEN."
expected = "unpack::execute returns a decrypt authentication failure and the expected extracted safe path does not exist."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-domain-keyslot-corruption"
group = "phase01-domain-keyslot-assurance"
path = "dexios-domain/tests/keyslots_v1.rs::wrong_key_current_v1_fixture_rejects_verification_and_decrypt"
purpose = "Link existing generated domain keyslot wrong-key assurance into the Phase 01 provenance corpus."
invariant = "D-04"
requirement = "ASSR-02"
source = "Generated inside dexios-domain keyslots_v1 with disposable in-memory keys and payload; no checked-in secret material."
expected = "Key verification returns IncorrectKey and decrypt returns DecryptMasterKey without accepting plaintext."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-domain-archive-path"
group = "phase01-domain-archive-path-assurance"
path = "dexios-domain/tests/unpack.rs::unpack_rejects_duplicate_targets_after_path_normalization"
purpose = "Link existing domain archive-path normalization and duplicate-target assurance into the Phase 01 provenance corpus."
invariant = "D-04"
requirement = "ASSR-02"
source = "Generated by the domain unpack integration test from a disposable ZIP archive containing normalized duplicate target paths."
expected = "unpack::execute rejects the archive with DuplicateOutputPath and does not extract the colliding target."
owner_phase = "Phase 1"

[[fixture]]
id = "phase01-domain-generated-corruption-promoted"
group = "phase01-generated-case-promotion"
path = "dexios-domain/tests/decrypt_workflow_errors.rs::decrypt_corrupted_stream_variants_never_commit_final_output"
purpose = "Prove generated domain corruption variants are promoted into normal deterministic Cargo regressions rather than live random exploration."
invariant = "D-07"
requirement = "ASSR-03"
source = "Deterministic final-tamper, one-byte-truncation, and reordered-normal-chunk mutations are fixed in source and replayed by decrypt_workflow_errors."
expected = "The generated corruption variants are exercised through cargo test --locked --offline -p dexios-domain --test decrypt_workflow_errors --release."
owner_phase = "Phase 1"
